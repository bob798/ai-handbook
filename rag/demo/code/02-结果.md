════════════════════════════════════════════════════════════
 STEP 1 ／ 对同一文档，应用三种分块策略，看分块结果
════════════════════════════════════════════════════════════

  文档总长度: 860 字符  (430 个汉字约）

────────────────────────────────────────────────────────────
  策略: A｜固定120字，无 overlap（太小，演示问题）
  结果: 共 8 块  |  平均 107 字符/块
  Chunk[0](120字): "RAG系统架构与工程实践指南  一、什么是RAG RAG是Retrieval-Augmented Genera..."
  Chunk[1](120字): "大模型，让模型基于真实资料生成回答。RAG解决了大模型两个核心痛点：知识截止日期和私有领域知识缺失。  二、分..."
  Chunk[2](120字): "片中理解上下文，答案质量大幅下降。分块太大（如2000个字符）：召回的块包含太多无关内容，LLM在长上下文中丢..."
  ... 还有 5 块

  策略: B｜固定200字 + 40字 overlap（推荐基础配置）
  结果: 共 6 块  |  平均 176 字符/块
  Chunk[0](200字): "RAG系统架构与工程实践指南  一、什么是RAG RAG是Retrieval-Augmented Genera..."
  Chunk[1](200字): "和私有领域知识缺失。  二、分块策略的重要性 文档分块是RAG系统中最容易被忽视但影响最大的工程决策。分块太小..."
  Chunk[2](200字): "200到500个字符之间，并设置约10%到20%的重叠区域（overlap）防止边界信息丢失。不同文档类型有不..."
  ... 还有 3 块

  策略: C｜句子感知，max=300字（语义最完整）
  结果: 共 3 块  |  平均 282 字符/块
  Chunk[0](300字): "RAG系统架构与工程实践指南一、什么是RAGRAG是Retrieval-Augmented Generatio..."
  Chunk[1](293字): "工程实践建议：推荐分块大小在200到500个字符之间，并设置约10%到20%的重叠区域（overlap）防止边..."
  Chunk[2](254字): "四、RAG评估指标评估RAG系统需要同时关注检索和生成两个维度。检索维度：召回率（Recall）衡量相关文档有..."


════════════════════════════════════════════════════════════
 STEP 2 ／ Overlap 的作用：保护边界信息
════════════════════════════════════════════════════════════

  目标关键句（含具体建议数值）:
  「推荐分块大小在200到500个字符之间，并设置约10%到20%的重叠区域」

  策略A（120字无overlap）：关键句完整存在？ ✅ 是
  策略B（200字有overlap）：关键句完整存在？ ✅ 是
  策略C（句子感知）：      关键句完整存在？ ✅ 是

════════════════════════════════════════════════════════════
 STEP 3 ／ 关键实验：分块策略 → 检索质量 → 回答质量
════════════════════════════════════════════════════════════

  问题: RAG 的分块策略有什么工程建议？推荐用多大的分块和 overlap？

  （这个问题的答案在文档「二、分块策略」章节中有具体数值）

────────────────────────────────────────────────────────────
  策略 A｜固定 120 字，无 overlap（块太小）
────────────────────────────────────────────────────────────
  命中的 Top-2 块：
  [0.6712] "大模型，让模型基于真实资料生成回答。RAG解决了大模型两个核心痛点：知识截止日期和私有领域知识缺失。  二、分块策略的重要性 文档分块是RAG系统中最容易被忽视..."
  [0.574] "片中理解上下文，答案质量大幅下降。分块太大（如2000个字符）：召回的块包含太多无关内容，LLM在长上下文中丢失焦点，答案精度降低。工程实践建议：推荐分块大小在..."

  🤖 回答:
  RAG的分块策略的工程建议是推荐分块大小在200到500个字符之间，并设置约10%到20%的重叠区域（overlap）以防止相关信息丢失。

────────────────────────────────────────────────────────────
  策略 B｜固定 200 字 + 40 字 overlap（推荐配置）
────────────────────────────────────────────────────────────
  命中的 Top-2 块：
  [0.7522] "和私有领域知识缺失。  二、分块策略的重要性 文档分块是RAG系统中最容易被忽视但影响最大的工程决策。分块太小（如50个字符）：每块缺乏完整语义，LLM无法从碎..."
  [0.6795] "RAG系统架构与工程实践指南  一、什么是RAG RAG是Retrieval-Augmented Generation的缩写，中文叫检索增强生成。核心思路是：在..."

  🤖 回答:
  推荐的分块大小在200到500个字符之间，并设置约10%到20%的重叠区域（overlap）以防止信息丢失。

────────────────────────────────────────────────────────────
  策略 C｜句子感知分块（语义最完整）
────────────────────────────────────────────────────────────
  命中的 Top-2 块：
  [0.698] "RAG系统架构与工程实践指南一、什么是RAGRAG是Retrieval-Augmented Generation的缩写，中文叫检索增强生成。核心思路是：在大模型..."
  [0.5784] "工程实践建议：推荐分块大小在200到500个字符之间，并设置约10%到20%的重叠区域（overlap）防止边界信息丢失。不同文档类型有不同的最优分块策略：技术..."

  🤖 回答:
  推荐分块大小在200到500个字符之间，并设置约10%到20%的重叠区域（overlap）防止边界信息丢失。


════════════════════════════════════════════════════════════
 V2 总结：分块决策框架
════════════════════════════════════════════════════════════

  分块工程决策表：

  ┌──────────────┬──────────┬────────────┬────────────────┐
  │ 策略          │ 实现复杂度│ 语义完整性  │ 推荐场景        │
  ├──────────────┼──────────┼────────────┼────────────────┤
  │ 固定大小无重叠 │ 极低     │ 低（易切断）│ 快速原型        │
  │ 固定大小有重叠 │ 低       │ 中          │ 通用文档，生产基线│
  │ 句子感知      │ 中       │ 高          │ 技术文档/规范    │
  │ 语义分块      │ 高       │ 最高        │ 高精度场景（v3+）│
  └──────────────┴──────────┴────────────┴────────────────┘

  参数推荐：
    chunk_size  →  200~500 字符（视文档类型调整）
    overlap     →  chunk_size 的 10%~20%
    chunk_size 和 overlap 没有通用最优解，需要用 v8 的评估框架来度量。

  你现在理解的概念：
    ✓ chunk_size：决定每块的信息密度，影响检索粒度
    ✓ overlap：防止边界信息丢失，代价是块数量略增
    ✓ 句子感知分块：牺牲块大小一致性，换取语义完整性
    ✓ "分块太小" 和 "分块太大" 造成的问题是不同的

  ⚠️  此刻的问题：我们是靠"肉眼看输出"判断哪种策略更好。
  这是不可靠的——你的直觉可能是错的，换一个文档结论就变了。
  → V3.5 要解决：构建黄金数据集，用 Recall@K 数字说话，
    让每次分块决策都有可对比的量化依据。

  V2 的局限（V3 要解决）：
    ✗ chunks 只存在内存里，程序一退出就消失
    ✗ 每次都要重新计算所有 chunk 的 embedding（慢且费钱）
    → V3 把 chunks + embeddings 持久化到向量数据库（ChromaDB）